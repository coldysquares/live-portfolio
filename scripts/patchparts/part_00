from pathlib import Path
import re

path = Path('index.html')
text = path.read_text(encoding='utf-8')
original = text


def replace_once(old: str, new: str, label: str) -> None:
    global text
    if old not in text:
        raise RuntimeError(f'Missing exact anchor: {label}')
    text = text.replace(old, new, 1)
    print(f'[ok] {label}')


def sub_once(pattern: str, repl: str, label: str, flags: int = re.S) -> None:
    global text
    updated, count = re.subn(pattern, repl, text, count=1, flags=flags)
    if count != 1:
        raise RuntimeError(f'Expected one regex match for {label}; got {count}')
    text = updated
    print(f'[ok] {label}')


# ---------------------------------------------------------------------------
# 1. Performance, metadata, and responsive shell
# ---------------------------------------------------------------------------
replace_once(
    '    <!-- Supabase JS Client CDN Library for Real-Time Full-Stack Persistence -->\n'
    '    <script src="https://jsdelivr.net"></script>',
    '    <meta name="description" content="ChronosAudit and Nick Berghane’s interactive browser-tool portfolio: local file hashing, multitrack audio mixing, interface prototypes, and technical consulting.">\n'
    '    <meta name="theme-color" content="#09090b">\n'
    '    <link rel="preconnect" href="https://fonts.googleapis.com">\n'
    '    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>\n'
    '    <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:wght@700;800&family=JetBrains+Mono:wght@400;500;700&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">',
    'remove broken blocking CDN and add metadata',
)
sub_once(
    r"\s*@import url\('https://fonts\.googleapis\.com/css2\?family=.*?display=swap'\);\s*",
    "\n",
    'remove render-blocking CSS import',
)

style_additions = r'''

        /* Revenue-ready responsive and utility additions */
        button, input, textarea, select { font: inherit; }
        button:focus-visible, input:focus-visible, textarea:focus-visible, select:focus-visible {
            outline: 2px solid #818cf8;
            outline-offset: 2px;
        }
        .mobile-nav-select {
            display: none;
            background: #141416;
            color: #d4d4d8;
            border: 1px solid #27272a;
            border-radius: 0.5rem;
            padding: 0.55rem 0.7rem;
            font-family: 'JetBrains Mono', monospace;
            font-size: 10px;
            font-weight: 700;
        }
        .tool-actions { display: flex; gap: 0.5rem; flex-wrap: wrap; }
        .stem-file-label {
            display: block;
            cursor: pointer;
            background: #18181b;
            color: #d4d4d8;
            border: 1px solid #27272a;
            border-radius: 0.5rem;
            padding: 0.6rem;
            text-align: center;
            font-family: 'JetBrains Mono', monospace;
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
        }
        .stem-file-name {
            min-height: 2.4em;
            color: #71717a;
            font-family: 'JetBrains Mono', monospace;
            font-size: 9px;
            line-height: 1.35;
            overflow-wrap: anywhere;
        }
        #mixer-strips-grid { grid-template-columns: repeat(4, minmax(0, 1fr)) !important; }
        .service-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.75rem; }
        .project-card-link { color: #a5b4fc; text-decoration: none; font-family: 'JetBrains Mono', monospace; font-size: 10px; font-weight: 700; }
        .project-card-link:hover { color: #fff; text-decoration: underline; }
        .field-label { color: #a1a1aa; font-size: 10px; text-transform: uppercase; font-weight: 700; letter-spacing: 0.04em; }
        @media (max-width: 64rem) {
            .glass-nav { height: auto; min-height: 72px; padding: 0.7rem 0; }
            .glass-nav .wrapper-layout { gap: 0.75rem; }
            #global-nav-links { display: none !important; }
            .mobile-nav-select { display: block; }
            #mixer-strips-grid { grid-template-columns: repeat(2, minmax(0, 1fr)) !important; }
        }
        @media (max-width: 42rem) {
            .wrapper-layout { padding-inline: 1rem; }
            .glass-nav .wrapper-layout { flex-wrap: wrap; }
            .glass-nav .btn-prime { flex: 1 1 auto; }
            .mobile-nav-select { flex: 1 1 10rem; }
            #mixer-strips-grid, .service-grid { grid-template-columns: 1fr !important; }
            .premium-card, .lab-console-panel { padding: 1rem; }
        }
'''
replace_once('    </style>', style_additions + '\n    </style>', 'append responsive CSS')

replace_once('Sovereign Compute Suite', 'Interactive systems &amp; consulting', 'plain-language brand tagline')
replace_once(
    '            <button onclick="UIControls.showModal(\'modal-consultation\')" class="btn-prime">Project Planner</button>',
    '''            <select id="mobile-nav-select" class="mobile-nav-select" aria-label="Choose a section" onchange="AppRouter.navigate(this.value)">
                <option value="home">ChronosAudit</option>
                <option value="mixer">Wollohy Studio</option>
                <option value="playgrounds">Interaction Labs</option>
                <option value="geometry">Canvas Graph</option>
                <option value="about">Work With Nick</option>
            </select>
            <button type="button" onclick="UIControls.showModal('modal-consultation')" class="btn-prime">Start a Project</button>''',
